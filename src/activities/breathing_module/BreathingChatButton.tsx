import React from 'react'

export default function BreathingChatButton() {
    /* This Button will contain an ICON to show on the main chat 
     onClick will send the nav rauter to game
     */
  return (
    <div onClick={()=>{
        return
    }}>BreathingChatButton</div>
  )
}

